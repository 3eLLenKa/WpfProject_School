<Window x:Class="WpfProject_School.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfProject_School"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        Title="Проект" Height="600" Width="920" MouseDown="MovingWindow">

    <Viewbox Stretch="Fill">
        <Grid>
            <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden"/>

            <Button x:Name="CloseButton" Click="CloseClient" VerticalAlignment="Top" HorizontalAlignment="Right" Style="{StaticResource CloseButtonStyle}" Margin="876,9,10,560" />
            <Button x:Name="MinimizeButton" Click="MinimizeClient" VerticalAlignment="Top" HorizontalAlignment="Right" Style="{StaticResource MinimizeButtonStyle}" Margin="0,9,64,0"/>
            <Button x:Name="MaximizeButton" Click="MaximizeClient" VerticalAlignment="Top" HorizontalAlignment="Right" Style="{StaticResource MaximizeButtonStyle}" Margin="0,9,37,0" RenderTransformOrigin="0.603,0.639"/>
        </Grid>
    </Viewbox>
</Window>